# .zprofile - login shell setup (runs before .zshrc)
# Note: brew shellenv moved to .zshenv so non-interactive shells (e.g. mosh) can find it

{{ if eq .chezmoi.os "linux" -}}
# Fix TERM for proper terminal handling
export TERM="${TERM:-xterm-256color}"
{{ end -}}
